import 'package:flutter/material.dart';

class GoodsDetail extends StatelessWidget {
  const GoodsDetail(Map<String, String> map, { Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('sss'),
    );
  }
}